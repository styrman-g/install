# install
install scripts for linux

## How to use this scripts
If you want to install sway on top of a debian standard install use:
* debian-sway-install.sh

### Notes
you ned to have a desctop enviroment with a displaymanager allready installd because the script will not install it.
